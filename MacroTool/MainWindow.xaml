<Window x:Class="MacroTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Macro Tool"
        Height="360"
        Width="384"
        Background="White"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Topmost="True">
    <Window.Resources>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#8B0A14"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#8B0A14"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="26" ResizeBorderThickness="5" CornerRadius="0" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>
    <Border BorderBrush="Black" BorderThickness="2" Background="White">
        <DockPanel LastChildFill="True">
            <!-- Header -->
            <Border DockPanel.Dock="Top" Background="#E5E7EB" BorderBrush="Black" BorderThickness="0,0,0,2" Height="26">
                <Grid Margin="6,0">
                    <TextBlock Text="Macro Tool" VerticalAlignment="Center" FontWeight="Bold" FontSize="11" Foreground="Black"/>
                    <Button Style="{StaticResource CloseButtonStyle}" WindowChrome.IsHitTestVisibleInChrome="True" HorizontalAlignment="Right" Width="18" Height="18" Click="Close_Click" Padding="0" ToolTip="Close">
                        <TextBlock Text="×" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,-2,0,0" FontSize="14" FontWeight="Bold"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Footer -->
            <Border DockPanel.Dock="Bottom" Background="#F3F4F6" BorderBrush="Black" BorderThickness="0,2,0,0" Height="24">
                <Grid Margin="6,0">
                    <TextBlock x:Name="lblStatus" Text="Ready" VerticalAlignment="Center" FontSize="10" Foreground="Black"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <TextBlock Text="Active: " FontSize="10" Foreground="Black"/>
                        <TextBlock x:Name="lblActiveWindow" Text="Macro Tool" FontSize="10" FontWeight="Bold" Foreground="Black"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Controls Section -->
            <Border DockPanel.Dock="Top" Background="#F9FAFB" BorderBrush="Black" BorderThickness="0,0,0,2" Padding="8">
                <StackPanel>
                    <Grid Margin="0,0,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <ComboBox x:Name="cmbWindows" Height="22" FontSize="10" BorderBrush="Black" BorderThickness="1" IsEditable="True" Padding="4,0" VerticalContentAlignment="Center" TextBoxBase.TextChanged="cmbWindows_TextChanged"/>
                            <TextBlock Text="Select window..." IsHitTestVisible="False" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="Gray" FontSize="10">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=cmbWindows}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        <Button Grid.Column="1" Click="btnRefresh_Click" Width="22" Height="22" Margin="4,0,0,0" Background="White" BorderBrush="Black" BorderThickness="1">
                            <TextBlock Text="↻" FontSize="12" FontWeight="Bold" Margin="0,-2,0,0"/>
                        </Button>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="40"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Column="0">
                            <TextBox x:Name="txtKeys" Height="22" FontSize="10" BorderBrush="Black" BorderThickness="1" IsReadOnly="True" Padding="4,0" VerticalContentAlignment="Center" LostFocus="txtKeys_LostFocus"/>
                            <TextBlock Text="Keys" IsHitTestVisible="False" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="Gray" FontSize="10">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=txtKeys}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                        <Button x:Name="btnRecordKeys" Grid.Column="1" Content="Set" Click="btnRecordKeys_Click" Height="22" Margin="4,0" Padding="4,0" FontSize="10" FontWeight="Bold" Background="Transparent" BorderBrush="Black" BorderThickness="1"/>
                        <TextBox Grid.Column="2" x:Name="txtInterval" Text="1000" Height="22" FontSize="10" BorderBrush="Black" BorderThickness="1" TextAlignment="Center" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="3" Click="btnAdd_Click" Content="Add" Width="40" Height="22" Margin="4,0,0,0" Background="Black" Foreground="White" FontWeight="Bold" FontSize="10" BorderThickness="0"/>
                    </Grid>
                    <!-- Record keys button integrated or separate? Mockup shows "Set Keys" might be needed if recording logic requires it -->
                    <Button x:Name="btnRecordKeysHidden" Content="Set Keys" Click="btnRecordKeys_Click" Height="20" FontSize="10" Background="White" BorderBrush="Black" BorderThickness="1" Width="60" HorizontalAlignment="Left" Visibility="Collapsed"/>
                </StackPanel>
            </Border>

            <!-- Table Section -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Table Header -->
                <Border Background="#F3F4F6" BorderBrush="Black" BorderThickness="0,0,0,1" Height="22">
                    <Grid Margin="6,0" ToolTipService.InitialShowDelay="10" ToolTipService.ShowDuration="5000">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="35"/>
                            <ColumnDefinition Width="35"/>
                            <ColumnDefinition Width="35"/>
                            <!-- Scrollbar spacer -->
                            <ColumnDefinition Width="{x:Static SystemParameters.VerticalScrollBarWidth}"/> 
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Target" FontWeight="Bold" FontSize="10" VerticalAlignment="Center" ToolTip="Target Window Title"/>
                        <TextBlock Grid.Column="1" Text="Keys" FontWeight="Bold" FontSize="10" VerticalAlignment="Center" ToolTip="Keys to Send"/>
                        <TextBlock Grid.Column="2" Text="Interval" FontWeight="Bold" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center" ToolTip="Interval (ms)"/>
                        <TextBlock Grid.Column="3" Text="Status" FontWeight="Bold" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center" ToolTip="Current Status"/>
                        <TextBlock Grid.Column="4" Text="Active" FontWeight="Bold" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center" ToolTip="Enable/Disable Macro"/>
                        <TextBlock Grid.Column="5" Text="Focus" FontWeight="Bold" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center" ToolTip="Focus Window First"/>
                        <TextBlock Grid.Column="6" Text="HW" FontWeight="Bold" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center" ToolTip="Hardware Input Mode"/>
                    </Grid>
                </Border>

                <!-- Scrollable List -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Visible">
                    <ItemsControl x:Name="gridMacros" AlternationCount="2">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border x:Name="RowBorder" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="6,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="35"/>
                                            <ColumnDefinition Width="35"/>
                                            <ColumnDefinition Width="35"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Column 0: Target + Buttons -->
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,4,0">
                                            <TextBlock Text="{Binding TargetWindowTitle}" FontSize="10" TextTrimming="CharacterEllipsis" FontWeight="SemiBold"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                <Button Content="Test" Click="btnTest_Click" FontSize="9" Padding="4,1" Background="White" BorderBrush="Black" BorderThickness="1" Margin="0,0,4,0"/>
                                                <Button Content="Del" Click="btnDelete_Click" FontSize="9" Padding="4,1" Background="White" BorderBrush="Black" BorderThickness="1"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Keys Column (Editable/Recordable) -->
                                        <TextBox Grid.Column="1" Text="{Binding FriendlyKeyText}" FontSize="10" VerticalAlignment="Center" 
                                                 IsReadOnly="True" Cursor="Hand" BorderThickness="0" Background="Transparent"
                                                 PreviewMouseLeftButtonDown="RowKeys_Click" Tag="{Binding}"/>

                                        <!-- Interval Column (Editable) -->
                                        <TextBox Grid.Column="2" Text="{Binding IntervalMs}" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center"
                                                 BorderThickness="0" Background="Transparent" TextAlignment="Center"/>

                                        <TextBlock Grid.Column="3" Text="{Binding Status}" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        
                                        <!-- Checkboxes -->
                                        <CheckBox Grid.Column="4" IsChecked="{Binding IsRunning}" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center" BorderBrush="Black" BorderThickness="1"/>
                                        <CheckBox Grid.Column="5" IsChecked="{Binding FocusOnSend}" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center" BorderBrush="Black" BorderThickness="1"/>
                                        <CheckBox Grid.Column="6" IsChecked="{Binding UseHardwareInput}" Width="14" Height="14" VerticalAlignment="Center" HorizontalAlignment="Center" BorderBrush="Black" BorderThickness="1"/>
                                    </Grid>
                                </Border>
                                <DataTemplate.Triggers>
                                    <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                                        <Setter TargetName="RowBorder" Property="Background" Value="White"/>
                                    </Trigger>
                                    <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                        <Setter TargetName="RowBorder" Property="Background" Value="#F9FAFB"/>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                        <Setter TargetName="RowBorder" Property="Background" Value="LightSalmon"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </DockPanel>
    </Border>
</Window>